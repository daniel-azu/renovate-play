import type { VersioningApi } from '../../../../versioning/types';
import type { LookupUpdateConfig } from './types';
export declare function getCurrentVersion(config: LookupUpdateConfig, versioning: VersioningApi, rangeStrategy: string, latestVersion: string, allVersions: string[]): string | null;
