"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getNodeConstraint = void 0;
const semver_1 = require("semver");
const logger_1 = require("../../../logger");
const fs_1 = require("../../../util/fs");
const node_1 = require("../../../versioning/node");
async function getNodeFile(filename) {
    try {
        const constraint = (await (0, fs_1.readLocalFile)(filename, 'utf8'))
            .split('\n')[0]
            .replace(/^v/, '');
        if ((0, semver_1.validRange)(constraint)) {
            logger_1.logger.debug(`Using node constraint "${constraint}" from ${filename}`);
            return constraint;
        }
    }
    catch (err) {
        // do nothing
    }
    return null;
}
function getPackageJsonConstraint(config) {
    var _a;
    const constraint = (_a = config.constraints) === null || _a === void 0 ? void 0 : _a.node;
    if (constraint && (0, semver_1.validRange)(constraint)) {
        logger_1.logger.debug(`Using node constraint "${constraint}" from package.json`);
        return constraint;
    }
    return null;
}
async function getNodeConstraint(config, allowUnstable = false) {
    const { packageFile } = config;
    let constraint = (await getNodeFile((0, fs_1.getSiblingFileName)(packageFile, '.nvmrc'))) ||
        (await getNodeFile((0, fs_1.getSiblingFileName)(packageFile, '.node-version'))) ||
        getPackageJsonConstraint(config);
    let lockfileVersion = 1;
    try {
        const lockFileName = (0, fs_1.getSiblingFileName)(packageFile, 'package-lock.json');
        lockfileVersion = JSON.parse(await (0, fs_1.readLocalFile)(lockFileName, 'utf8')).lockfileVersion;
    }
    catch (err) {
        // do nothing
    }
    // Avoid using node 15 if node 14 also satisfies the same constraint
    // Remove this once node 16 is LTS
    if (constraint) {
        if ((0, semver_1.validRange)(constraint) &&
            (0, semver_1.satisfies)('14.100.0', constraint) &&
            (0, semver_1.satisfies)('15.100.0', constraint) &&
            !(0, node_1.isStable)('16.100.0')) {
            if (lockfileVersion === 2) {
                logger_1.logger.debug('Forcing node 15 to ensure lockfileVersion=2 is used');
                constraint = '>=15';
            }
            else if ((0, semver_1.validRange)(`${constraint} <15`)) {
                logger_1.logger.debug('Augmenting constraint to avoid node 15');
                constraint = `${constraint} <15`;
            }
        }
    }
    else if (allowUnstable && lockfileVersion === 2) {
        logger_1.logger.debug('Using node >=15 for lockfileVersion=2');
        constraint = '>=15';
    }
    else {
        logger_1.logger.debug('No node constraint found - using latest');
    }
    return constraint;
}
exports.getNodeConstraint = getNodeConstraint;
//# sourceMappingURL=node-version.js.map